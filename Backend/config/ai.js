// Configuración del SDK de Gemini AI
let genAI = null;
let geminiModel = null;
if (process.env.GOOGLE_API_KEY) {
    // Aquí iría la inicialización real del SDK de Gemini
    // genAI = ...
    // geminiModel = ...
}
module.exports = { genAI, geminiModel };
