// Enrutador principal que une todos los routers
const express = require('express');
const router = express.Router();
// Aquí se importarán y montarán los routers de cada módulo
module.exports = router;
